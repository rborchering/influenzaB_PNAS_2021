opts_chunk$set(
    ## set default (inches), over-ride as needed
    fig.height=5,
    ## over-ride latex page layout / float positioning
    fig.pos='h!',
    ## don't show code
    echo=F,
    ## change to F for final
    warning=F,
    message=F
)
